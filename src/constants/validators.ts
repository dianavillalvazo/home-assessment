const checkConsecutiveLetters = /([a-z])\1/i;
const checkNumbers = /\d/;
const checkSpecialCharacter = /([!@#$%^&*])/;
const checkUppercase = /([A-Z])/;

export {
  checkConsecutiveLetters,
  checkNumbers,
  checkSpecialCharacter,
  checkUppercase,
};
